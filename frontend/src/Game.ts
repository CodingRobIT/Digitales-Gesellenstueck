export type Game = {
    id: string,
    titel: string,
    publisher: string,
    genre: string,
    note: string
}
export type NewGame = {
    titel: string,
    publisher: string,
    genre: string,
    note: string
}