export type Game = {
    id: string,
    title: string,
    publisher: string,
    genre: string,
    note: string,
}
export type NewGame = {
    title: string,
    publisher: string,
    genre: string,
    note: string,
}